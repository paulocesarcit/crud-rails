<%= form_with(model: crud, local: true) do |form| %>
  <% if crud.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crud.errors.count, "error") %> prohibited this crud from being saved:</h2>

      <ul>
      <% crud.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :id_seq %>
    <%= form.text_field :id_seq %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
